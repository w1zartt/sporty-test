spring:
  application:
    name: test
    domain-properties:
      sport-events-source:
        url: http://localhost:8081
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      properties:
        schema.registry.url: http://localhost:8094
    consumer:
      properties:
        schema.registry.url: http://localhost:8094
    properties:
      security.protocol: PLAINTEXT